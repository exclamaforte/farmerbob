MERGED glm-53-flash
PASS, 856 tests, clippy 0, 766L. Makes the subjective tier work on a ONE-ARM field: critique
casts from the roster via stage_cast instead of requiring two implementations.
codex-luna critiqued it and found a REAL DEFECT, verified and fixed on merge: repo_file fell
back to the hard-coded repository whenever the configured path was not a file, so an FB_REPO
pointing at a tree with no sources.toml silently read the REAL roster and cast critics from
it. FB_REPO is the isolation override; the fallback defeated exactly that while making a
misconfigured run indistinguishable from a correct one.
This arm also earned its continuation: its first run was killed at 45 minutes by a deadlock
in its own test suite, and the follow-up turn found and fixed it. 22 critique tests now pass
in 0.02s, up from 17 on the base.
