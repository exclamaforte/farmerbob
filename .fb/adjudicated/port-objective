VOID -- the one candidate failed the differential
Adjudicated 2026-09-17. Three of four arms produced nothing; the fourth, or-luna-pro, built
clean, was scope clean, and passed its 2 tests -- then failed the differential on two counts.
It emits 220 rows where the shell emits 137, and it renames the `crates` field to
`crates_touched`. The rename is a real defect: fb-objective.sh:123 renames deliberately on the
way out (`"crates": e.get("crates_touched")`) and the port passed the raw name through, into
the file `fb pareto`, `fb select` and the bandit's posteriors all read.
Not merged. The row-count divergence alone would have changed every downstream metric.
The run also overwrote logs/objective.json before it was known to be correct; restored from a
copy taken beforehand, and filed as farmerbob-jd2.14 -- the differential must run against a
redirected output path, which is the FB_LOGS override a critic asked for on port-critique and
I did not act on.
