MERGED or-hy3
Adjudicated 2026-09-17, second attempt. 17 tests to or-inkling's 4 and or-luna-pro's 2, all
three PASS and scope clean. Its output is FIELD-FOR-FIELD IDENTICAL to fb-objective.sh run
fresh: 225 rows, same run set, no differing field on any row.
CORRECTION TO THE FIRST ADJUDICATION. That attempt was voided for emitting "220 rows where the
shell emits 137". It was never compared against the shell. It was compared against a STALE
objective.json that had not been regenerated since several tasks ran. The shell, run now,
emits 225. The arm was right and I was wrong, and I wrote that false defect into the spec
where it sat until this run reproduced it identically from two independent arms -- N-of-N,
which is the signal that the fault is not in the field.
The `crates`/`crates_touched` rename WAS real, verified against fb-objective.sh:123, and both
surviving ports honour it now that the spec pins it.
Credited: or-inkling for a run_cmd(logs, wtroot, repo, only) signature that takes its paths as
arguments -- the injection a critic asked for on port-critique, and the only one of the three
that is testable against a fake tree without environment variables.
