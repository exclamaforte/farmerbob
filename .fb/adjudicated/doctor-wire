MERGED codex-luna
Single arm, 162 lines, 3 tests, clippy 0.

fb doctor now runs orphan_check on every invocation. Live output:

  orphaned modules   Ok   no orphaned modules found across 4 crates

The arm separated SCANNING from DECIDING as the spec asked, so the decision is
tested against constructed CrateSources values rather than against this
repository's current state, which changes every merge. It delegates: two calls
into orphan_check and no second declaration scanner.

Clause 2 held: an orphan is Fail, not Warn. An uncompiled module is not a
suboptimal configuration, it is work that silently is not there.

Closes farmerbob-0m34 -- the condition is now asserted mechanically instead of
depending on someone noticing a test count drop from 583 to 541.
