#!/usr/bin/env bash
# Generated by hooks/install.sh. Owns core.hooksPath so beads cannot regenerate over it.
set -uo pipefail
beads="/home/gabe/Documents/farmerbob/.beads/hooks/pre-push"
[ -x "$beads" ] && { "$beads" "$@" || exit $?; }
exit 0
