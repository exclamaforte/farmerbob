#!/usr/bin/env bash
# Generated by hooks/install.sh. Owns core.hooksPath so beads cannot regenerate over it.
set -uo pipefail
beads="/home/gabe/Documents/farmerbob/.beads/hooks/pre-commit"
[ -x "$beads" ] && { "$beads" "$@" || exit $?; }
"/home/gabe/Documents/farmerbob/hooks/pre-commit-fmt" || exit 1
exit 0
